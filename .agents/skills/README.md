# Handler Skill Pack

This repository publishes a single public entrypoint skill designed for progressive disclosure:

- `handler`: umbrella workflow for installing and using handler, validating A2A agents, and running MCP checks.

Install with:

```bash
npx skills add alDuncanson/handler
```

Then follow the downlinks in `handler/SKILL.md` to load focused competency references.
